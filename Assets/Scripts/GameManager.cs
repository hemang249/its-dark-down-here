/**
 * This script will be used to keep track of counts and other data related to collectables, fear level, time in dark, etc.
 * This script will interface with UIManager.
 **/



using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
